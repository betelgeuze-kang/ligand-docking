# Optional API server profile, not required for offline local delivery runtime.
# These versions are the CPU-tested API compatibility set. FastAPI 0.137+
# represents included routers as a tree, so route-contract tests use the
# public iter_route_contexts compatibility helper.
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
prometheus-client==0.25.0
httpx2==2.5.0
