# TASK-ID: v2-openmm-fixed-born-failure-disposition

## Goal

Classify the two observed fixed-Born OpenMM native endpoint failures with a preregistered, failure-preserving solver and constraint-projection diagnostic.

## Scope

- Bind the exact rejected 14-case native endpoint receipt and its source materialization.
- Reproduce both fixed-Born aliases and retain the original tangent-force and constraint thresholds unchanged.
- Record a fixed iteration-budget, optimizer-tolerance, and constraint-tolerance probe matrix.
- Capture OpenMM minimizer callback counts, restraint-stage transitions, energy trace, constraint error, and pre/post final-projection endpoints.
- Produce one canonical, tamper-evident, exact-reexecution diagnostic receipt with an ordered evidence classification.
- Keep the frozen endpoint rejected and all S0/S1, chemistry, benchmark, product, and causal-root-cause claims false.

## Likely Files

- `betelgeuze_engine_v2/offline/openmm_reference_fixed_born_disposition.py`
- Engine v2 package entry point, focused tests, and scientific status documentation

## Verification

- Frozen diagnostic configuration identity and exact two-case scope.
- Baseline native receipt reproduction, full probe coverage, alias consistency, reporter trace accounting, pre/post projection metrics, classification, tamper, file-mode, no-overwrite, CLI, and exact reexecution tests.
- Focused OpenMM/result-review regressions, Ruff, compileall, package build/install, architecture guard, and `git diff --check`.

## Stop Conditions

- Do not relax a threshold, replace the frozen endpoint result, infer unavailable optimizer rejection counts, provision trust material, invent a second host, or promote S0/S1/product claims.

## Risk Level

R3
