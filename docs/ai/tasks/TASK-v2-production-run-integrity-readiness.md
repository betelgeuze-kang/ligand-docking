# TASK-ID: v2-production-run-integrity-readiness

## Goal

Make both synthetic validation entrypoints capable of issuing truthful, reproducible production-class receipts without weakening the pre-import trust boundary.

## Scope

- Replace the false `-I` plus `PYTHONHASHSEED` evidence with a source-bound, controlled re-exec boundary that proves the actual interpreter hash seed while retaining stdlib-only startup, no site/PYTHONPATH injection, exact argv, and source-only imports.
- Add 27/59 dependency executable/distribution/stdlib byte observation and revalidation at bootstrap, parent, and fixed worker boundaries, matching or exceeding the 14-case chain.
- Bind a durable per-file dependency manifest plus the actually mapped native DSO closure, reject unrecorded importable payloads, and retain worker pre/post identity evidence.
- Put dependency/source preflight under one carried cooperative deadline before any nonce or runner-start marker is consumed, then close directory/RECORD traversal gaps before describing it as a hard end-to-end wall bound.
- Make the 27/59 checkout declaration truthful, then require an externally provisioned root-owned read-only source snapshot before any production-class execution.
- Add a signed evidence class and custody identity across review, authorization, environment, observation, result, and response schemas so test-only artifacts remain closed while a fully verified production run records `production_validation_results_collected=true` without opening scientific/fitting/product claims.
- Remove unconditional test-only/production-missing blockers only when the production evidence class and complete external chain verify.
- Preserve the implemented 27/59 independent post-result-review contract with full case/variant/metric/failure dispositions and role-separated signature verification while keeping actual review evidence externally provisioned and claim-closed.

## Current Progress

- Both stdlib-only bootstraps now fail closed on root execution and unless the complete Engine v2 package tree is under a canonical root-owned, group/other-nonwritable, symlink-free source snapshot; user-owned files, hardlinks, and bytecode caches are rejected before package import. They independently rehash the signed raw Git commit and recursive tree objects with Git SHA-1 object framing, then compare the exact tracked `betelgeuze_engine_v2` path set and every regular file's mode, blob OID, SHA-256, and size with the live root-owned read-only tree. The canonical source manifest is passed as the sixth bootstrap-state element.
- Source and dependency preflight now carry monotonic deadlines and aggregate entry/file/byte budgets. Directory walks use bounded `scandir`, wheel `RECORD` rows are streamed directly, and payloads are size-capped before reads; normalized install-scheme confinement, active-import/RECORD binding, namespace closure, and bytecode rejection remain enforced. These are bounded preflight traversals, not kernel-enforced end-to-end lifetime isolation.
- Both lanes now generate canonical full per-file source and dependency manifests while preserving the signed commit and six aggregate dependency digests. Run-start persists `<nonce>.source-tree.json` and `<nonce>.dependencies.json` with mode 0600, `O_EXCL`, `O_NOFOLLOW`, and file/directory fsync. Runner and result finalization require exact persisted/live equality and bind the source-manifest digest through environment, runner-start, observation, and result identities; minimization result-review validation and its Ed25519 signature also bind that digest.
- The active energy-force base chain uses v2 identities with a v4 runner/result writer. The refrozen minimization base chain uses v4 review/execution-environment identities, v5 authorization/result-receipt/nonce/run-start identities, a v8 runner, and v7 result writer/result review after binding the v2.1 trajectory-comparison protocol. The dependent production review/authorization and reservation identities are v3; registry-proof, authenticated-head, later-head, and witness-quorum identities remain v2, and the adjacent epoch-transition continuity identity is v1. Current hashes are recomputed through their upstream dependency DAG. A read-only registry verifies 64 superseded contract documents by canonical projection hash. It intentionally does not claim compatibility with superseded signed attestations, receipts, or run records.
- Both fixed workers now emit exact request-bound `pre -> payload -> post/completion` lifecycle evidence. The durable observation retains the canonical request transport, supervised child PID, transcript digest/length/frame sequence, and bounded incomplete-output disposition. The live supervisor requires raw stdout to equal the canonical transcript reconstructed from the request, ordered retained rows, and lifecycle; writers and both result reviews independently reparse the request and reconstruct/re-hash the successful transcript. Incomplete output retains a bounded prefix digest/length and discard disposition, accepts no child payload, and is never review-acceptable. A separate Linux primitive now measures PID, parent PID (including namespace-init zero), stat-field-22 start tick, boot ID, and PID-namespace inode from fixed `/proc` with bounded no-follow reads and race checks. It is not yet bound into worker carriers, cannot exclude same-tick PID reuse, and is not durable uniqueness or external launch authenticity.
- The energy-force lane now has a frozen Ed25519 post-result-review leaf contract. It validates the exact receipt and ordered 27-case/59-variant/19-metric evidence, independently recomputes all 56 required metric occurrences from retained raw energy/force arrays with bitwise retained-value equality, derives case/variant/metric/failure/worker dispositions, verifies four-role separation, and keeps every promotion flag false. No production result receipt, result-review attestation, trusted result-reviewer key, or independent human approval is bundled. The upstream scientific-review and authorization records remain symmetric HMAC, and the leaf does not independently reverify the live dependency manifest or establish external custody.
- A common Ed25519 base foundation freezes the exact `synthetic_validation_production` evidence class, pre-execution permit, monotonic status snapshot, bounded canonical transport, and the raw sequence-1 permit/sequence-2 status custody prefix without changing custody-v1. An additive companion internally re-verifies that prefix and implements production-only Ed25519 sequence-3 review and sequence-4 authorization carriers/events, including exact scalar-type, causal-time, global identity/key/material separation, and raw/logical revocation checks. Permit consumption and successor uniqueness are not enforced; no key, carrier/event, permit, external log, one-use registry, enrolled host, actual custody chain, or immutable store is provisioned. The process-launch digest is bound but not externally authenticated, and the energy-force upstream review/authorization remains symmetric HMAC.
- A sequence-5 companion now re-verifies the complete exact raw sequence-1-through-4 prefix and either lane's canonical local reservation record. Its custodian-signed intent binds realm-global permit/nonce/predecessor slots and exact registry/witness identity, key, epoch, and prior checkpoint. The commit artifact verifies registry and witness signatures plus a strictly newer post-commit status descendant, but remains attestation-only: actual external serializable CAS, one-use slot consumption, non-equivocation, epoch continuity, and custody-successor uniqueness are all false. Same-prior-head sibling attestations remain possible. No registry, key, intent, commit proof, or production artifact is provisioned.
- A verifier-only external same-epoch registry transaction proof now freshly re-verifies sequence 5, validates a fixed-order chain of exactly three adjacent transaction-tagged sparse-Merkle leaf transitions, verifies distinct backend/head-observer signatures, supplied freshly reverified status-lineage-tail denials, causal time, and exact backend runtime identities, and requires a backend-native checkpoint to equal a caller-supplied expected sequence/checkpoint. A supplied proof verifies only the backend's serializable/committed attestation, the exact three-leaf transition, the observer-signed checkpoint, and equality with that caller expectation. It does not authenticate the expectation's provenance or prove that the supplied status tail is globally latest. Actual external CAS, global one-use consumption, status-head CAS, non-equivocation, epoch continuity, later-head consistency, successor uniqueness, and execution remain false; no proof, backend, keys, or authenticated head receipt is provisioned.
- A verifier-only authenticated head/status receipt boundary now snapshots both registry-proof argument graphs before use, reproduces the same raw proof against the receipt-time and post-receipt status lineages, verifies a role-separated external Ed25519 authority over the exact proof/sequence-5/head/status/service/time/challenge projection, and requires a strict current-status descendant issued after the receipt. The later tail can revoke or supersede the exact receipt, authority, proof, checkpoint, and service identities. This proves bounded signature, exact binding, and caller challenge equality only; challenge freshness/one-use, global latest, CAS, global slot consumption, later-head consistency, non-equivocation, epoch continuity, and successor uniqueness remain false. No receipt, authority key, challenge, or post-receipt current-status descendant is provisioned.
- A verifier-only same-epoch later-head consistency boundary now freshly re-verifies the authenticated receipt, requires a bounded sequence-adjacent backend-signed checkpoint/state-root path, verifies the head observer over the full ordered path, and reconstructs sparse-Merkle inclusion of the anchor-attested transaction-tagged consumed-leaf encodings in the caller-pinned later root. Proof issue cannot predate the anchor receipt, `later_head_observed_at_utc` means observer countersign completion, and a strict post-proof status tail applies denials. The DTO preserves false challenge-freshness/one-use and actual slot-consumption facts. It proves one supplied fork only: separately pinned siblings can each pass, so global latest, external non-equivocation, epoch continuity, CAS, execution, and promotion remain false. No proof or post-proof status is provisioned.
- A fixed-policy witness-quorum boundary now verifies N/F/Q and `2Q-N>F`, the exact ordered full roster with distinct caller-pinned declared witness/operator/fault-domain identifiers, a stable exact-anchor fork scope, and Q Ed25519 votes over one exact descendant lineage. All N members are validity- and denial-fenced. The result is conditional and anchor-scoped: actual fault-bound compliance, exclusive-vote locking, independent journal agreement, hidden sibling-certificate absence, realm-wide non-equivocation, and epoch continuity remain unverified. No policy, keys, certificate, journals, or post-quorum status is provisioned.
- A verifier-only adjacent registry-epoch transition boundary now freshly re-verifies the exact previous witness-quorum proof, requires `next_ordinal = previous_ordinal + 1`, preserves the previous terminal state root as sequence-zero genesis, derives the genesis checkpoint from the complete context, and verifies disjoint previous/next fixed-roster Ed25519 quorums over the same statement. This proves only one supplied transition's continuity. Separately quorum-signed sibling transitions can each verify without external locking, so successor uniqueness, independent journal agreement, realm-wide non-equivocation, global latest, CAS, and every promotion claim remain false. No transition proof, next policy, keys, votes, or post-transition status is provisioned.
- `validation_runtime_integrity_contract.py` v13 additionally binds the exact refrozen minimization trajectory-comparison contract alongside custody-v1 and the current v3 review/authorization-extension and reservation-extension, v2 external registry-proof, authenticated head/status receipt, same-epoch later-head consistency and fixed-policy anchor-scoped witness quorum, v1 adjacent epoch-transition continuity, plus the process-launch-identity frozen SHA-256 value. It records the implemented comparison/checkpoint and transition-verifier scope while explicitly keeping production comparison receipt, provisioned registry/transition evidence, kernel vDSO content identity, procfs superblock authentication, authorized native allowlisting, execution-lifetime closure, worker binding, same-tick collision exclusion, external launch authenticity/custody, production carriers, successor uniqueness, and an end-to-end asymmetric upstream chain false. Runtime v8 through v12 remain read-only legacy identities; the registry now contains 64 superseded documents.
- Still open: a provisioned external registry proof/backend/head receipt/later-head/quorum/adjacent-transition proof and post-proof/post-transition status descendants, independently fresh/one-use caller challenge, externally enforced witness locking and independent journal comparison, externally provisioned root-owned source/dependency runtime, kernel-backed source/Git-metadata immutability and custody, pre-bootstrap stdlib closure, signed native allowlisting and full load/execute/unload lifetime closure, vDSO/kernel identity, binding measured worker launch identity into signed carriers, same-tick collision-resistant external launch nonce/custody, final environment→start→worker-transcript→observation→result→review→response production carriers, actual energy-force result receipt/review/attestation/trusted key, independent dependency-manifest re-verification at result review, replacement of the upstream symmetric-HMAC chain, actual 27/59 and 14-case production runs, two-host reproduction, accepted production trajectory disposition/external comparison, and human independent-review approval. All production, scientific, fitting, S0/S1, and product claims remain false.

## Non-goals

- No keys, trust stores, dependency installation, production execution, second-host/external result, S0 acceptance, S1 work, fitting, or product/scientific promotion.

## Likely Files Or Search Targets

- Both validation bootstrap, dependency identity, authorization, receipt, run-start, runner, writer, and result-review chains
- 27/59 result-review module, focused tests, capability/status/roadmap/CI records

## Verification

- Repeated fresh-process hash determinism, environment/path injection, direct-stage bypass, source/dependency/checkout cross-wire, evidence-class downgrade, test/production confusion, metric/failure omission, signature/role/revocation/supersession tests.
- Both complete validation chains, runtime-integrity companion contract, Ruff, capability/YAML equality, architecture guard, and `git diff --check`.

## Stop Conditions

- Follow `AGENTS.md`; do not read `.env` files or mutate external state.
- Stop before setting any scientific, fitting, S0/S1, or product claim from receipt collection alone.

## Risk Level

R4
